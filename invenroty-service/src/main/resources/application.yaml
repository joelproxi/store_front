spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/inventory_ms
    username: proxi
    password: 123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update

server:
  port: 9501
  servlet:
    context-path: /api/v1


#springdoc:
#  api-docs:
#    enabled: true
#    path: /inventories/v3/api-docs
#  swagger-ui:
#    enabled: true
#    path: /api/v1/inventories/swagger-ui.html
#

springdoc:
 api-docs:
   enabled: true
   path: /v3/api-docs
 swagger-ui:
   enabled: true
   path: /swagger-ui.html


eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
  instance:
    preferIpAddress: true

